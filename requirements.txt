# Pinned to current, security-patched versions (verified working on Python 3.12).
# Only direct dependencies are listed; pip resolves transitive packages to
# their current safe releases.

# Core web UI
gradio==5.50.0

# LLM / orchestration (modern split-package LangChain)
langchain==0.3.30
langchain-core==0.3.86
langchain-openai==0.3.35
langchain-text-splitters==0.3.11
openai==2.44.0

# Speech-to-text for the behavioural audio feature
transformers==5.12.1
torch==2.12.1

# Data / numerics
numpy==2.2.6
pandas==2.3.3
matplotlib==3.11.0

# Config + external data
python-dotenv==1.2.2
youtube-transcript-api==1.2.4
